name: 'rebuild-deploy'
on:
  workflow_dispatch:
  push:
    branches:
      - main
      
jobs:
  test-runner:
    runs-on: [self-hosted]
    steps:
      - name: Checkout
        uses: actions/checkout@v2.5.0
        with:
          path: /home/alex/Documents/Github