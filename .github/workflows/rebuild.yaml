name: 'rebuild-deploy'
on:
  workflow_dispatch:
  push:
    branches:
      - main
      
jobs:
  test-runner:
    runs-on: [self-hosted]
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Update flake
        run: nix flake update
      - name: print directory
        run: pwd
      #- name: Rebuild laptop
      #  run: nixos-rebuild switch --flake .#laptop 